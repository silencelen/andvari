{
  "name": "andvari-extension",
  "version": "0.6.0",
  "private": true,
  "description": "andvari browser extension (MV3) — PC autofill. Pure-JS @noble crypto (no WASM/eval).",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/ciphers": "^2.2.0",
    "@noble/hashes": "^2.2.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
